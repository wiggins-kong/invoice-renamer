# PRODUCT.md — 发票识别重命名

## 产品是什么
本地桌面工具（Electron）：识别 PDF 发票内容（发票号码、日期、销售方、价税合计等 9 个字段），
按用户用可视化拼接器自由组合的命名模板，批量**原地重命名**发票文件，支持一键撤销。
可选 LLM（DeepSeek / OpenCode Go / 自定义 OpenAI 兼容端点）做字段补全。

## 用户与场景
财务/个人用户批量整理电子发票 PDF（数电票、增值税电子/专用发票）。发票文件散落在下载文件夹、
桌面、公司目录；用户在收到一批发票后集中归档重命名。单机使用，数据不出本机。

## 核心任务流
1. 拖入文件/文件夹（或原生选择器）→ 2. 自动识别字段并实时按模板生成新文件名 →
3. 检查/微调表格 → 4. 全部重命名 → （可撤销）

## 关键事实（不可破坏）
- 解析：pdfjs 文本提取 + 正则（双栏版式兼容）；金额提取不依赖 ¥ 符号
- 原地重命名，不产生副本；绝不覆盖（冲突自动加后缀）；撤销日志
- 界面三主题：亮色 / 暗色 / 跟随系统（**高 DPI 缩放下必须清晰锐利**）
- LLM：提取模式三选一（仅正则/混合/仅LLM）；提供商可切换；模型下拉自动抓取
- 命名模板：可视化拼接器（拖拽排序、预设、实时预览、缺失字段自动跳过）
- 结果列表：合并追加、单行移除、清空；重命名后只原位更新对应行

## 品牌承诺（用户钉选，2026-08-16）
- **视觉世界：明亮清爽的现代工作台**——浅色优先（白 + 蓝，轻盈通透，Notion/Linear 气质），
  同时提供精心反转的暗色主题与跟随系统
- **布局：双栏工作台**——左侧窄栏（设置 + 命名模板），右侧主区（文件 + 识别结果表格）
- 克制配色（中性色 + 单一蓝色强调色）；系统字体栈；状态色语义化
- 目标：工具感消失，任务本身成为焦点（Operate 模式）
